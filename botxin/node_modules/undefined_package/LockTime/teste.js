const unde = require('undefined_package')

const locked = unde.locktime('17:30', '01:59', 'Fora Do Horário', 'Dentro Do Horário')

if(locked.isLocked) { console.log(locked.text) } else { console.log(locked.text) }
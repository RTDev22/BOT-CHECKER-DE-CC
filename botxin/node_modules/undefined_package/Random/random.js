function random(string) {
    if(!string) return 'Insira Algo Para Randomizar, Exemplo: unde.random(["Batata", "Cenoura", "Abacate"])'
    let random = string[Math.floor(Math.random()*string.length)]
    return random.trim()
  }

  module.exports = random